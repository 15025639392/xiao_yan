# 世界事件进入 GPT 上下文 设计

## 目标

让数字人在回答用户时，不只依赖聊天记忆，还会把最近相关的世界事件带进模型上下文。

## 第一版规则

- 仅把 `kind="world"` 的记忆作为上下文补充，不混入聊天历史保存逻辑
- 世界事件以 `system` 消息进入模型输入
- 优先使用与当前用户提问相关的世界事件

## 结果

模型在回答时会同时看到：
- 最近相关的聊天记忆
- 最近相关的世界事件
- 当前用户消息
