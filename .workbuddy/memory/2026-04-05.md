# 2026-04-05 工作日志

## Phase 5: 回滚恢复 + 健康检查（自愈闭环）完成 ✅

### 新增文件
- `rollback_recovery.py` — RollbackRecovery, DiffSnapshot, RollbackPlan, RollbackResult 等核心类
- `health_checker.py` — HealthChecker 5 维度评分 + HealthReport 综合报告
- `test_phase5_rollback_health.py` — 53 个测试全覆盖

### 修改文件
- `models.py` — 新增 health_score/health_grade/rollback_info/snapshot_taken 字段
- `executor.py` — 集成 pre-apply 快照 + smart_rollback API
- `service.py` — APPLIED 后自动健康评估 + 回滚建议标记

### 测试结果
- **Phase 5: 53 passed, 0 failed**
- **全量: 275 passed, 0 failed**（完全向后兼容）

### 踩过的坑
1. `HealthDimensionScore` 构造缺少 `weighted_score` 必填参数（批量修复脚本产生残留代码）
2. `_score_all_dimensions` 中 lambda 闭包变量未定义就引用（改为延迟构建）
3. executor.py 中 apply 后的二次快照覆盖了 pre-apply 正确快照（移除二次快照）
4. `DEGRADEING` typo → `DEGRADING`
5. `detail` vs `details` 参数名错误
6. pycache 缓存导致旧代码残留（需清理）

## 前端可观测性增强（Step 1 完成）✅

### 新增/修改文件
- **`lib/api.ts`** — 类型定义从 10 字段扩到 25+（补齐 Phase 2-5），新增 EditKind/SelfImprovementEdit/HealthReport/HistoryEntry 等类型，新增 fetchHistory/rollback/fetchHealth API
- **`StatusPanel.tsx`** — 完全重写自编程面板：
  - 可折叠面板 + 健康度圆环（SVG 动画）
  - Diff 查看器（REPLACE/CREATE/INSERT 三种编辑类型，before/after 高亮）
  - 触碰文件标签列表
  - Git 信息展示（分支/commit/message）
  - 安全检查状态（沙箱预检 + 冲突检测）
  - 回滚操作区 + 二次确认弹窗（危险弹窗样式）
  - 冷却倒计时
- **`HistoryPanel.tsx`** — 全新组件：自编程历史记录列表页
  - 加载/空状态/error 处理
  - 时间相对格式化、持续时间计算
  - 健康度颜色编码、回滚标记
- **`App.tsx`** — 新增 history 路由 + HistoryPage 组件 + 回滚处理函数
- **`index.css`** — 新增 ~400 行 CSS（si-panel / health-ring / diff-viewer / rollback / history-list / modal 等）

### 设计决策
1. 自编程面板默认展开详情（用户进来就能看到全部信息）
2. Diff 查看器默认折叠（避免信息过载，点击才展开代码）
3. 回滚需要二次确认弹窗（防止误操作）
4. 历史记录作为独立路由页面（不塞在总览里太拥挤）

### TypeScript 编译：0 错误

## Phase 6: 审批交互系统（Step 2 完成）✅

### 后端改动
- **`domain/models.py`** — SelfImprovementStatus 新增 `PENDING_APPROVAL` + `REJECTED` 枚举值；SelfImprovementJob 新增 `approval_requested_at` / `approval_edits_summary` / `approval_reason` 字段
- **`main.py`** — 新增 3 个审批 API 端点：
  - `GET /self-improvement/pending` — 查询当前待审批 Job
  - `POST /self-improvement/{job_id}/approve` — 批准（推进到 VERIFYING）
  - `POST /self-improvement/{job_id}/reject` — 拒绝（标记 REJECTED + 记录原因）
- **`service.py`** — PATCHING 阶段后插入 PENDING_APPROVAL 门控（不再直接进入 VERIFYING）；tick_job 中 PENDING_APPROVAL 状态不自动推进；新增 `_build_edits_summary()` 辅助函数

### 前端改动
- **`lib/api.ts`** — status 联合类型新增 pending_approval/rejected；Job 类型新增 3 个审批字段；新增 approve/reject/fetchPending API 函数
- **`components/ApprovalPanel.tsx`** — 全新审批面板组件：
  - 审批摘要信息（目标/原因/方案/补丁摘要）
  - 触碰文件列表
  - 可折叠 Diff 展示（精简版，三种编辑类型）
  - 批准按钮（大主操作）+ 拒绝按钮（带原因表单）
  - 批准/拒绝后展示结果状态
- **`StatusPanel.tsx`** — 当 job.status === "pending_approval" 时渲染 ApprovalPanel；新增 onApprovalDecision prop
- **`App.tsx`** — 新增 handleApprovalDecision 回调函数，传递给 StatusPanel → OverviewPanel 链路；状态映射补全 pending_approval/rejected
- **`HistoryPanel.tsx`** — 状态映射补全
- **`index.css`** — 新增 ~280 行 CSS（approval-panel / approval-summary / approval-files / approval-diff / approval-actions / reject-form）

### 核心流程
1. 数字人自编程完成 apply 后 → 状态变为 `pending_approval`
2. 前端 StatusPanel 检测到该状态 → 渲染 ApprovalPanel 替代 SelfImprovementPanel
3. 用户查看变更内容（文件列表 + Diff）→ 点击批准或拒绝
4. 批准 → 后端推进到 VERIFYING（后续自动验证）；拒绝 → REJECTED 终态

### TypeScript 编译：0 错误

## Phase 6 收尾 + 测试修复（续做完成）✅

### 发现的问题
1. **main.py 缺少导入**：`Any`、`logger`、`datetime`、`WorldRepository`/`WorldStateService` → 7 个测试文件无法 collect
2. **test_self_improvement_service.py 7 个测试失败**：PENDING_APPROVAL 门控导致旧断言失败
3. Phase 6 缺少专项测试文件

### 修复内容
1. main.py 补齐所有缺失导入
2. test_self_improvement_service.py：7 个测试适配审批门控 + _approve_pending_job() 辅助函数
3. 新增 test_phase6_approval.py（14 个测试）：审批 API 全流程、状态门控、辅助函数

### 最终结果
- 后端: **288 passed, 0 failed**（275 → 288，+13）
- 前端: **TypeScript 0 错误**

## Phase 7: 人格内核系统 ✅

### 核心设计：让数字人有"性格"

**问题**：Day 1 的数字人回复全是模板拼出来的，聊 100 句也感觉不到"他是谁"

**解决方案**：完整的人格档案系统，从数据模型到 prompt 注入全链路

### 新增文件（后端 4 个 + 前端 3 个 + 测试 1 个）

**后端核心：**
- `persona/models.py` — 完整人格数据模型（~500 行）：
  - `PersonaProfile`: 名字/身份/背景故事 + 性格五维度(OCEAN) + 说话风格 + 价值观 + 动态情绪状态
  - `PersonalityDimensions`: 大五模型 (openness/conscientiousness/extraversion/agreeableness/neuroticism)
  - `SpeakingStyle`: 正式程度/句式偏好/表达习惯/口头禅/回复长度
  - `EmotionalState`: 主次情绪 + 情感效价(mood_valence) + 唤醒度(arousal) + 活跃事件列表
  - `EmotionEntry`: 单条情绪事件（类型/强度/原因/来源/衰减时间）
  - 12 种情绪类型（Ekman 六基本情绪 + 扩展），5 级强度
  - `default_persona()`: 「小晏」出厂设置——温和好奇略内向的数字人

- `persona/emotion_engine.py` — 情绪累积与衰减引擎：
  - 事件驱动：外部事件 → apply_event() → 更新 active_entries → 重算主导情绪
  - 时间衰减：tick() → 每级强度自然衰减 → 回归 calm 基线
  - 性格影响：高神经质放大波动，高外向增强 joy/surprise，高宜人性减弱愤怒
  - 多源推断：聊天消息启发式识别、目标事件映射、自编程事件映射
  - 情感效价计算：综合 valence (-1~1) + arousal (0~1) 二维模型

- `persona/service.py` — PersonaService 统一门面：
  - CRUD：get/update_profile, update_personality, update_speaking_style, reset_to_default
  - 情绪操作：apply_emotion/tick_emotion/infer_chat_emotion/infer_goal_emotion/infer_si_emotion
  - Prompt 生成：build_system_prompt()（完整人格注入）/ build_persona_prompt()（向后兼容）
  - JSON 文件持久化（FilePersonaRepository）

- `persona/prompt_builder.py` — 改造为支持 persona 注入（向后兼容旧接口）

**前端组件：**
- `components/PersonaCard.tsx` — 状态面板人格卡片：
  - 名字 + 版本号 + 当前情绪 badge（emoji + 颜色编码）
  - 心情条（mood_valence 可视化，-1~1 映射到颜色和宽度）
  - 性格特质标签（自动从大五维度生成中文描述）
  - 最近情绪事件流（来源标签化：chat/goal/self_improvement/manual）
  - 30 秒自动刷新情绪状态

- `components/PersonaSettings.tsx` — 设置页人格配置面板：
  - Tab 切换：基础信息 / 性格维度 / 说话风格
  - 基础信息：名字/身份/背景故事编辑
  - 性格：5 维度滑块（0~100，实时预览，渐变色轨道反馈）
  - 说话风格：正式程度(5档)/表达习惯(5种)/回复长度(4档)/口头禅输入
  - 重置默认按钮（二次确认）
  - Toast 提示反馈

- `src/persona.css` — ~400 行 Persona 专用样式

**测试：**
- `tests/test_phase7_persona.py` — 59 个测试全覆盖：

### 修改文件
- `config.py` — 新增 get_persona_storage_path()
- `main.py` — 大量改动：
  - 新增 import：Persona 相关类型 + FilePersonaRepository + PersonaService
  - _ensure_runtime_initialized: 初始化 persona_repository + persona_service
  - 新增 get_persona_service() 依赖注入
  - `/chat` 端点：注入人格 system_prompt + 聊天时自动推断情绪
  - 新增 11 个 API 端点：
    - GET /persona — 获取完整人格档案
    - GET /persona/summary — 获取摘要（前端用）
    - PUT /persona — 更新基础信息
    - PUT /persona/personality — 更新性格维度
    - PUT /persona/speaking-style — 更新说话风格
    - POST /persona/reset — 重置默认
    - GET /persona/emotion — 查询情绪状态
    - POST /persona/emotion/apply — 手动触发情绪事件
    - POST /persona/emotion/tick — 手动推进衰减
    - GET /persona/prompt — 调试用：查看当前 system prompt
- `lib/api.ts` — 新增 ~200 行 TypeScript 类型定义 + 10 个 API 函数
- `StatusPanel.tsx` — 集成 PersonaCard 组件
- `SettingsPanel.tsx` — 集成 PersonaSettings 组件
- `main.tsx` — 引入 persona.css

### 测试结果
- **Phase 7: 59 passed, 0 failed**（新增）
- **全量: 347 passed, 0 failed**（288 → 347，+59，零回归）

### 关键设计决策
1. **大五模型 (OCEAN)** 作为性格基础——心理学上成熟、可量化、易于映射到行为
2. **情绪效价二维模型 (valence × arousal)** ——比离散标签更能驱动 prompt 差异化
3. **事件驱动的情绪累积** ——不是即时计算的，有持续性和衰减
4. **性格影响情绪反应幅度** ——同事件不同性格的人反应不同
5. **完全向后兼容** ——没有 persona_service 时回退到旧的硬编码逻辑
6. **JSON 文件持久化** ——重启不丢失人格配置（但情绪状态仍随时间衰减）
7. **PromptBuilder 双模式** ——有 persona 注入时使用动态 prompt，否则 fallback 到 Xiao Yan 硬编码

## Phase 8: 记忆与人格联动系统 ✅

### 核心设计：让数字人从"失忆的聊天机器人"变成"有记忆的个体"

**问题**：Phase 7 让数字人有了性格，但每次对话都"失忆"。用户说了 preferences、约定、名字，下次完全记不住。

**解决方案**：结构化记忆系统 + 人格感知检索 + 对话自动提取

### 新增文件（后端 2 个 + 前端 2 个 + 样式 1 个 + 测试 1 个）

**后端核心：**

- `memory/models.py` — 重构为完整记忆数据模型（~350 行）：
  - `MemoryEntry`（替代旧 MemoryEvent）：5 种类型(fact/episodic/semantic/emotional/chat_raw) + 5 级强度(faint→core) + 情绪标签 + 关键词 + 重要性(0-10) + 访问次数
  - `MemoryCollection`：集合视图，支持按 kind 过滤、按重要性排序、转 prompt 片段
  - `MemoryKind/MemoryStrength/MemoryEmotion` 枚举
  - `retention_score` 属性：0~1 保留分值 = 强度基础 + 重要性加权 + 访问频率加成
  - 向后兼容：保留旧 `MemoryEvent`，新增 `from_entry()` / `to_entry()` 互转方法

- `memory/service.py` — MemoryService 统一门面（~380 行）：
  - CRUD：save/create/list_recent/search（带人格感知重排序）
  - **对话驱动提取**：`extract_from_conversation()` 自动识别用户偏好("我喜欢"/"我叫")/约定("明天"/"记得")/情绪词 → 存为对应类型记忆
  - 人格感知检索：高开放性偏好事实知识，高神经质不容易忘记负面记忆，高宜人性偏好正面记忆
  - Prompt 集成：`build_memory_prompt_context()` 搜索相关记忆 + 重要事实精选 → 注入 system prompt
  - 记忆生命周期：strengthen/weaken_old_memories（概念性操作）
  - 统计摘要：get_memory_summary/get_memory_timeline

**前端组件：**

- `components/MemoryPanel.tsx` — 记忆面板组件（~280 行）：
  - 头部：图标 + 统计摘要 + 类型过滤 chips（可点击筛选）
  - 搜索框：实时搜索记忆（防抖 300ms）
  - 时间线列表：每条显示类型图标/badge + 角色(user/小晏) + 强度指示器 + 内容(3行截断) + 关键词标签 + 情绪色点 + 保留分值进度条
  - 骨架屏加载态 + 空状态提示
  - 30 秒自动刷新

- `src/memory.css` — ~320 行记忆面板专用样式：
  - 类型统计 chips 可点击切换高亮
  - 搜索框聚焦发光效果
  - 记忆条目 hover 高亮 + 图标缩放动画
  - 保留分值渐变进度条
  - 自定义细滚动条
  - 骨架屏 shimmer 动画

**测试：**
- `tests/test_phase8_memory.py` — 43 个测试全覆盖

### 修改文件

- **`main.py`** 改动：
  - 新增 import: MemoryService, 新记忆类型枚举
  - `_ensure_runtime_initialized()`: 初始化 memory_service（注入 persona_service 的 personality）
  - 新增 `get_memory_service()` 依赖注入函数
  - `/chat` POST 端点改造：
    - 调用 `build_memory_prompt_context()` 获取记忆上下文
    - 将 `memory_context` 传给 `build_chat_instructions()`
    - 回复后调用 `extract_from_conversation()` 自动提取结构化记忆
  - 新增 8 个 API 端点：
    - GET /memory/summary — 记忆系统统计摘要
    - GET /memory/timeline — 记忆时间线（前端展示）
    - GET /memory/search?q= — 搜索记忆
    - POST /memory — 手动创建记忆
    - GET /memory/recent — 最近记忆（可按类型过滤）
    - GET /memory/context?query= — 调试用：查看注入 prompt 的记忆上下文
    - POST /memory/weaken-old — 淡化旧记忆

- **`persona/prompt_builder.py`** — `build_chat_instructions()` 新增 `memory_context` 参数，追加到 prompt 末尾

- **`lib/api.ts`** — 新增 ~120 行 TypeScript 类型定义 + 8 个 API 函数（MemoryKind/Strength/Emotion/EntryDisplay 等）

- **`StatusPanel.tsx`** — 在 PersonaCard 下方集成 MemoryPanel 组件

- **`main.tsx`** — 引入 memory.css

### 测试结果
- **Phase 8: 43 passed, 0 failed**（新增）
- **全量: 390 passed, 0 failed**（347 → 390，+43，零回归）
- TypeScript: 0 错误

### 踩过的坑
1. `MemoryEvent.to_entry()` 的 kind 映射缺少新类型（fact/episodic/semantic/emotional/chat_raw）→ 所有新类型都 fallback 到 CHAT_RAW
2. `MemoryEvent` 不携带 importance 字段 → 从 repo 读回后 importance 默认 5 不够 >=6 过滤阈值 → fact 类型默认 importance 改为 6
3. `_generate_id()` 返回类型写了 `string` 但未导入 → 应该是 `str`
4. 测试中手动指定 id 的 entry created_at 几乎相同导致排序不确定 → 改用 sleep 分隔

## Phase 9: 情绪→表达风格深层映射系统 ✅

### 核心设计：让数字人的情绪真正影响说话方式

**问题**：Phase 7 建了完整的情绪引擎（12 种情绪 × 5 级强度 + 衰减 + 性格调节），但情绪只变成一句 system prompt 描述（"你现在比较开心"），并没有告诉 LLM **怎么因为情绪改变说话方式**。结果：用户调了半天性格参数，发现数字人开心和难过时说话感觉差不多。

**解决方案**：ExpressionStyleMapper — 将情绪状态转化为具体的"表达风格指令"

### 新增文件（后端 1 个 + 测试 1 个）

**后端核心：**

- `persona/expression_mapper.py` — 表达风格映射器（~550 行）：
  - **6 个表达维度枚举**：
    - `ResponseVolume` (5 级): very_brief → verbose → very_verbose
    - `EmojiLevel` (5 级): never → rarely → sometimes → often → frequently
    - `SentencePattern` (5 种): fragmented / short_direct / balanced / exclamatory / elaborate
    - `PunctuationStyle` (5 种): minimal / loose / standard / energetic / dramatic
    - `ToneModifier` (6 种): flat / gentle / playful / intense / hesitant / sarcastic
  - **12 种情绪完整映射表**：每种情绪定义基础风格 + 强烈模式(intense_instructions)
    - 正面组(JOY/GRATEFUL/PROUD/SURPRISE/ENGAGED)：话量偏多、emoji 丰富、句式活泼
    - 负面组(SADNESS/LONELY/ANGER/FRUSTRATED/FEAR/DISGUST)：话量减少、emoji 几乎不用、短句/省略
    - CALM：正常、温和
  - **ExpressionStyleMapper 类**：
    - `map_from_state(state)` → ExpressionStyleOverride（核心映射）
    - 双情绪融合：主+次要情绪按权重叠加
    - 性格调节因子：高神经质放大表达幅度，高宜人性缓和负面表达，高外向性增强正面表达，高尽责性保持条理
    - `build_style_prompt(override)` → 自然语言指令字符串
    - `build_full_expression_guide(state, base_style)` → 完整表达指南
  - 辅助方法：_amplify_emoji(提升 emoji 等级) / _blend_emoji(取较高者)

**测试：**
- `tests/test_phase9_expression.py` — 32 个测试（9 大类）：
  1. 基础映射（6 个）：每种情绪类型都有正确覆盖
  2. 强度影响（3 个）：MILD vs INTENSE 差异
  3. Prompt 生成（4 个）
  4. 性格调节（3 个）
  5. 双情绪融合（2 个）
  6. to_expression_prompt() 集成（4 个）
  7. SystemPrompt 集成（2 个）
  8. 工具函数（2 个）
  9. 边界情况（6 个）

### 修改文件

- **`persona/models.py`** 改动：
  - `EmotionalState.to_prompt_hints()` 保留（向后兼容）
  - 新增 `EmotionalState.to_expression_prompt(personality?)` 方法 — 返回表达风格指令
  - `PersonaProfile.build_system_prompt()` 重构：
    - 原流程：身份 → 性格 → 说话风格 → 价值观 → 情绪描述 → 核心原则
    - 新流程：身份 → 性格 → 说话风格 → 价值观 → 情绪描述 → ★**表达风格覆盖**★ → 核心原则
    - 自动创建 ExpressionStyleMapper 并注入【表达风格（受当前情绪影响）】段落

- **`persona/prompt_builder.py`** 改动：
  - `build_chat_instructions()` 新增 `expression_style_context: str | None = None` 参数
  - 在 memory_context 之后追加 expression_style_context

- **`main.py`** 改动：
  - 新增 import: ExpressionStyleMapper
  - `/chat` POST 端点改造：
    - 创建 `ExpressionStyleMapper(personality=persona_service.profile.personality)`
    - 调用 `map_from_state(current_emotion)` 获取风格覆盖
    - 调用 `build_style_prompt(override)` 生成指令文本
    - 将 `expression_style_context` 传给 `build_chat_instructions()`
  - 新增 API: `GET /persona/expression-style` — 返回当前情绪 + 风格配置 + 风格指令（前端展示 + 调试）

- **前端 `PersonaCard.tsx`** 增强（~330 行）：
  - 新增 fetchExpressionStyle() 调用（30 秒刷新，失败不阻塞）
  - 新增 **表达风格指示器面板**：
    - 头部：🎭 图标 + "当前表达风格" + 受 XX 情绪影响 badge
    - 4 维度网格（话量/表情/句式/语气）：图标 + 标签 + 当前值
    - 风格指令预览（斜体，80 字截断）
    - 渐变背景 + fade-in 动画 + hover 缩放效果
  - 仅在有活跃非平静情绪时显示（has_active_style 控制）

- **`lib/api.ts`** 扩展：
  - 6 个新类型：ExpressionVolume / EmojiLevelType / SentencePatternType / PunctuationStyleType / ToneModifierType / StyleOverrideConfig
  - ExpressionStyleResponse 类型（emotion + style_override + style_instruction + has_active_style）
  - `fetchExpressionStyle()` API 函数

- **`persona.css`** 扩展（+90 行）：
  - `.persona-card__expression-style` 渐变背景卡片
  - `.persona-card__style-dimensions` 2×2 网格布局
  - `.persona-card__style-dim` hover 缩放动画
  - `.persona-card__style-instruction` 斜体预览区
  - fade-in 动画

### 完整链路（修复后的）

```
用户消息 → infer_chat_emotion() → EmotionalState 更新
    ↓
map_from_state(emotional_state) → ExpressionStyleOverride
    ↓  (6 维度: volume/emoji/sentence/punctuation/tone)
    ↓  (性格调节: neuroticism/agreeableness/extraversion/conscientiousness)
    ↓  (双情绪融合: primary + secondary)
build_style_prompt(override) → 自然语言指令
    ↓
注入 build_chat_instructions(expression_style_context=...)
    ↓
LLM 收到完整 prompt → 不仅知道"心情怎样"，还知道"因为心情所以怎么说话"
    ↓
输出：开心时活泼多 emoji，难过时简短少 emoji，愤怒时直接尖锐
```

### 测试结果
- **Phase 9: 32 passed, 0 failed**（新增）
- **全量: 422 passed, 0 failed**（390 → 422，+32，零回归）
- TypeScript: 0 错误

### 踩过的坑
1. 中文省略号 `…`(U+2026) 在 Python 字符串中导致 SyntaxError → 替换为 `...`
2. 中文引号嵌套 `"xxx"` 导致 SyntaxError → 全部替换为转义的双引号 `\"xxx\""`
3. `"\n" + ".join(mods)` 少了 `\n`.join 的 `\n` → 字符串拼接错误
4. SADNESS 的 emoji_level 断言写死了 NEVER 但性格融合后变为 RARELY → 放宽断言为 NEVVER 或 RARELY
